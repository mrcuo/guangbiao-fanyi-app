# 维护规范

本文件定义公开产品仓库的日常维护和发布边界，不构成面向用户的付费服务等级承诺。

## 固定节奏

- 每个工作日：查看私密安全入口、P0/P1 问题和下载异常；
- 每周：Issue 分流、重复项合并、FAQ 和链接检查；
- 每次发布：完成安装包、文案、权限、隐私和校验值复核；
- 每月：检查过期内容、指标、支持成本和社区规则；
- 每季度：审计 GitHub 权限、发布凭据隔离和安全响应流程。

## Issue 分流

- P0：疑似用户数据泄露、凭据泄露、供应链问题；
- P1：无法安装、持续崩溃、核心翻译大面积不可用；
- P2：有替代方案的一般功能问题；
- P3：体验改进、低影响问题和建议。

`status:planned` 只表示已经进入维护安排，不代表公开发布日期。

## 内容治理

- README 只描述最新稳定版或明确标记的预发布版；
- 失效链接应在一周内修复或移除；
- 超过两个稳定版本的故障说明需要复核是否仍然有效；
- 路线探索不进入 README，也不以里程碑日期对外承诺；
- 负面反馈只要真实、相关且不侵犯他人，就应保留。

## 权限隔离

- 公开仓库的 GitHub Actions 不得持有可读取私有源码仓库的令牌；
- 不在 GitHub Secrets 中保存 Developer ID 私钥、公证账号主密码或生产 AI 主密钥；
- 发布账户启用 2FA，并使用最小权限和受保护环境；
- 安装包由受控私有构建流程产生，公开仓库只接收审核后的不可变产物和校验值。

## 版本规则

- 稳定版：`vX.Y.Z`；
- 测试版：`vX.Y.Z-beta.N`；
- 候选版：`vX.Y.Z-rc.N`；
- 版本文件发布后不可原地替换；任何二进制变化都必须使用新版本号和新校验值。

官网是用户认知中的官方下载入口，GitHub Releases 保存版本说明和可验证记录。官网/CDN 与 GitHub 提供同版本安装包时，文件必须字节一致。

